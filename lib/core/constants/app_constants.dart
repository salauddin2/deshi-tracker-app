class AppConstants {
  static const String baseUrl = 'https://desitracker-backend-main.vercel.app/api/v1/';
  static const String appName = 'DesiTracker';
  static const Duration connectTimeout = Duration(seconds: 15);
  static const Duration receiveTimeout = Duration(seconds: 30);
}
